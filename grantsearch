#!/usr/bin/env bash
#  grantsearch (root level wrapper)
#  どこからでも "grantsearch" コマンドで UI を起動できるシンプルなシェルラッパです。
#  プロジェクト内の python ランチャースクリプトを呼び出します。

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/google-adk/grantsearch_cli.py" "$@" 